# Non-runtime Snapshot

This directory archives artifacts that are not required for runtime, build, or core development workflows.

Current archived content:

- `analysis/`: reverse-engineering and exploratory artifacts
- `index-BNNYqckM.css`: generated root artifact
- `test-results/`: generated local test output snapshots

These files are kept for historical reference and can be removed from future releases if not needed.
