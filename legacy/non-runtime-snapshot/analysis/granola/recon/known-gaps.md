# Known Gaps

- Physical source map files are absent from extracted app bundle.
- Native `.node` modules are not decompiled in this pass.
- Screenshot automation now works at display level, but automated per-window captures are still limited.
- Channel extraction includes false positives from generic string scans.
